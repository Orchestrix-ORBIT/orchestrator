server:
  port: 8082

spring:
  application:
    name: realtime-service

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://db.nqmitpyrheqqrcbkwwut.supabase.co:5432/postgres?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:mmINekXXuCsuDloR}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
